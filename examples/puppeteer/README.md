# Puppeteer example

```bash
npm install
node index.mjs            # or: CDP_URL=http://127.0.0.1:9222 node index.mjs
```

The User-Agent and viewport are set per page — the container never sets them, so
every client must.
